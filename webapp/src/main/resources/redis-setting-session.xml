<?xml version="1.0" encoding="UTF-8"?>

<redis>
	<serverlist>
		<server 
			codisName="${codis.namespace.session}" address="${codis.zookeeper.address}" cluster="codis" maxActive="${codis.pool.maxActive}" maxIdle="${codis.pool.maxIdle}" name="SessionShare">
			<keylist>
				<key name="Session内容" value="SessionContent:{SessionId}" type="hash" />
				<key name="cas_ticket和sessionId" value="casTicket2sessionId:{casTicket}"
					type="String" />
			</keylist>
		</server>
	</serverlist>
</redis>